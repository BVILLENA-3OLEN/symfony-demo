{% set page_title = 'app.index.title' | trans %}
{% extends 'base.html.twig' %}

{% block body %}
    <article>
        <main>
            <p>Bienvenue aux 3OLEN 2025 sur ce module "Framework Symfony" !</p>
        </main>
    </article>

    <article>
        <header>
            <h2>Les articles publiés</h2>
        </header>

        <nav>
            <a
                href="{{ path('app_post_create_get') }}"
                class="btn btn-primary"
            >
                ➕ Nouveau
            </a>
        </nav>

        <main>
            <ul>
                {% for published_post in published_posts %}
                    <li>
                        <span class="bg-info">
                            {{ published_post.publishedAt | date('d/m/Y à H:i')}}
                        </span>

                        {{ published_post.title }}

                        <a href="{{ path('app_post_show_get', {
                            id: published_post.id
                        }) }}" class="btn btn-info">
                            👀
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </main>
    </article>

    {% if nom is not null %}
        <nav>
            <a href="{{ path('app_hello_name_get', {
                name: nom,
            }) }}" class="btn btn-primary">
                Bonjour {{ nom }} !
            </a>
        </nav>
    {% endif %}
{% endblock body %}
