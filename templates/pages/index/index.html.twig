{% set page_title = 'app.index.title' | trans %}
{% extends 'base.html.twig' %}

{% block body %}
    <article>
        <main>
            <p>Bienvenue aux 3OLEN 2025 sur ce module "Framework Symfony" !</p>
        </main>
    </article>

    {% if nom is not null %}
        <nav>
            <a href="{{ path('app_hello_name_get', {
                name: nom,
            }) }}" class="btn btn-primary">
                Bonjour {{ nom }} !
            </a>
        </nav>
    {% endif %}
{% endblock body %}
